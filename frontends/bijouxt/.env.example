# API base URL. In production, set to your Railway API URL (trailing slash).
# In development, leave unset to use the proxy (see package.json "proxy") — backend must run on port 4040.
REACT_APP_SERVER=https://your-api.up.railway.app/
